const getTags = async (req, res) => { res.json({ message: 'TODO: getTags' }); };
const createTag = async (req, res) => { res.json({ message: 'TODO: createTag' }); };

module.exports = { getTags, createTag };
